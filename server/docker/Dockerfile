# Use a minimal base image
FROM alpine:latest

# Install bash (or other interpreters like Python if needed)
RUN apk add --no-cache bash

# Set the working directory inside the container
WORKDIR /scripts

# Default command: runs bash (or change based on script type)
CMD ["bash"]
